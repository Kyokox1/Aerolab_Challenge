import React from "react";

import styles from "./banner.module.css";

export const Banner = () => {
	return (
		<section className={styles.banner}>
			<h3 className={styles.banner__text}>Electronics</h3>
		</section>
	);
};
